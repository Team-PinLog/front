# 실제 값은 .env에 복사해서 사용한다. .env는 gitignore되어 커밋되지 않는다.
VITE_API_BASE_URL=/api/core/v1

# 카카오 개발자 콘솔 앱 키. 프론트가 BFF 경유 없이 직접 호출한다(docs/api-contract.md Place·지도·검색).
# 보안은 .gitignore가 아니라 카카오 콘솔의 플랫폼(도메인) 등록에 의존한다 — 등록 도메인 외에는 키가 노출돼도 호출이 거부된다.
VITE_KAKAO_REST_KEY=
VITE_KAKAO_JS_KEY=
